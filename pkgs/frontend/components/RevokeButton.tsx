import { Button } from "@chakra-ui/react";

const RevokeButton = () => {
  return (
    <Button colorScheme="red" size="lg" mt={5} width="full">
      Revoke
    </Button>
  );
};

export default RevokeButton;
