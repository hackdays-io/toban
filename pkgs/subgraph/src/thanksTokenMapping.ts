export function handleTokenMinted(ev): void {}

export function handleTransfer(ev): void {
  // This function is intentionally left empty.
  // It can be used to handle transfer events in the future if needed.
}
